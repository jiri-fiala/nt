{
  "name": "test-nuxt",
  "version": "1.0.0",
  "description": "nuxt test app",
  "author": "Jiří Fiala",
  "private": true,
  "config": {
    "nuxt": {
      "host": "0.0.0.0",
      "port": "8080"
    }
  },
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt build && nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "nuxt": "^1.0.0",
    "bootstrap-vue": "^1.0.2",
    "bootstrap": "^4.0.0-beta.2"
  },
  "devDependencies": {
    "cross-env": "^5.0.1"
  }
}
